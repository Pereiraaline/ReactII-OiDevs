import styled, {css} from "styled-components";

// const background = css`
//   background-color: ${(props) => (props.background ? "#000" : "#fff")}; ;
// `;

export const Wrapper = styled.button`
  appearance: none;
  border: 0;
  cursor: pointer;
  padding: 8px;
  border-radius: 4px;
  div {
    color: #000;
  }
`;
